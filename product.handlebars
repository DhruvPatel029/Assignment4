<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
    <style>
        /* Add your CSS styles here */
        .product {
            border: 1px solid #ccc;
            margin-bottom: 20px;
            padding: 10px;
        }
        .product img {
            max-width: 100%;
            height: auto;
        }
    </style>
</head>
<body>
    <h1>All Products</h1>
    {{#each products}}
    <div class="product">
        <h2>{{title}}</h2>
        <img src="{{imgUrl}}" alt="{{title}}">
        <p>ASIN: {{asin}}</p>
        <p>Stars: {{stars}}</p>
        <p>Reviews: {{reviews}}</p>
        <p>Price: ${{price}}</p>
        <p>List Price: ${{listPrice}}</p>
        <p>Category: {{categoryName}}</p>
        <p>Best Seller: {{#if isBestSeller}}Yes{{else}}No{{/if}}</p>
        <p>Bought in Last Month: {{boughtInLastMonth}}</p>
    </div>
    {{/each}}
</body>
</html>
